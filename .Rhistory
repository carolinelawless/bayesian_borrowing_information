B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
coverage_interval <- 0.95
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
res$coverage
res$epsilons
params
res$epsilons[[10]]
mean(res$epsilons[[10]])
mean(res$epsilons[[9]])
mean(res$epsilons[[8]])
deltas <- seq(0,, 0.3, 7)
deltas
deltas <- seq(0, 0.3, 7)
deltas
?seq
deltas <- seq(0, 0.3, length.out = 7)
deltas
params <- c(rep(0.6, 9), 0.6 + delta)
delta <- deltas[7]
params <- c(rep(0.6, 9), 0.6 + delta)
params
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
res$epsilons
res$epsilons[[10]]
mean(res$epsilons[[10]])
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 7)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
eta_k_vec
plot(deltas, eta_k_vec)
plot(deltas, eta_k_vec
xlim = c(0, 1),
plot(deltas, eta_k_vec
ylim = c(0, 1),
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16
)
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ"
)
eta_k_vec
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ"
)
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 50
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 7)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 50
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
#
# res <- posterior_sim(
#   params = params,
#   M = M,
#   B = B,
#   lambdas = lambdas,
#   epsilon_const = epsilon_const,
#   CSD = CSD,
#   thres = thres,
#   model = "binomial",
#   model_params = binomial_params
# )
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 7)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 100
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
#
# res <- posterior_sim(
#   params = params,
#   M = M,
#   B = B,
#   lambdas = lambdas,
#   epsilon_const = epsilon_const,
#   CSD = CSD,
#   thres = thres,
#   model = "binomial",
#   model_params = binomial_params
# )
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 7)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 100
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
#
# res <- posterior_sim(
#   params = params,
#   M = M,
#   B = B,
#   lambdas = lambdas,
#   epsilon_const = epsilon_const,
#   CSD = CSD,
#   thres = thres,
#   model = "binomial",
#   model_params = binomial_params
# )
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 7)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 100
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
#
# res <- posterior_sim(
#   params = params,
#   M = M,
#   B = B,
#   lambdas = lambdas,
#   epsilon_const = epsilon_const,
#   CSD = CSD,
#   thres = thres,
#   model = "binomial",
#   model_params = binomial_params
# )
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 20)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
CSD
lambda <- 50
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 20)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 10
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 20)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "binomial",
model_params = binomial_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
remove(list = ls())
setwd("~/Documents/travail/dmd_when_next_clinical_trial?/bayesian_borrowing_information")
source("functions.R")
M <- 100
B <- 500
n_versions <- 10
a_theta <- b_theta <- 0.5
mean_theta <- sd_theta <- sd_obs <- 0.5
binomial_params <- list(
a_theta = a_theta,
b_theta = b_theta
)
gaussian_params <- list(
mean_theta = mean_theta,
sd_theta = sd_theta,
sd_obs = sd_obs
)
thres <- 0.1
thres2 <- 0.8
coverage_interval <- 0.95
#scenario <- "stable"
#scenario <- "gradual"
scenario <- "abrupt"
if(scenario == "stable"){
params <- rep(0.7, n_versions)
}else if(scenario == "gradual"){
params <- seq(0.6, 0.9, length = n_versions)
}else if(scenario == "abrupt"){
params <- c(rep(0.6, n_versions - 1), 0.9)
}
lambda <- 10
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 20)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "gaussian",
model_params = gaussian_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 50
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 20)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "gaussian",
model_params = gaussian_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 10
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 20)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "gaussian",
model_params = gaussian_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
lambda <- 100
#lambda <- 100
lambdas <- rep(lambda - 1, n_versions)
CSD <- 0.2 #TEA with EB
epsilon_const <- 0.5
eta_k_vec <- vector()
deltas <- seq(0, 0.3, length.out = 20)
for(delta in deltas){
params <- c(rep(0.6, 9), 0.6 + delta)
res <- posterior_sim(
params = params,
M = M,
B = B,
lambdas = lambdas,
epsilon_const = epsilon_const,
CSD = CSD,
thres = thres,
model = "gaussian",
model_params = gaussian_params,
coverage_interval = coverage_interval
)
eta_k_vec <- c(eta_k_vec,mean(res$epsilons[[10]]))
}
plot(deltas, eta_k_vec,
ylim = c(0, 1),
pch = 16,
ylab = "ηK",
xlab = "Δ",
main = paste0("λ = ", lambda)
)
